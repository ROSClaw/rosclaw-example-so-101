# so101_pose_registry

Robot-side named pose registry and live Cartesian stats utilities for the SO-101 follower arm.

This package exposes:

- persistent named pose storage
- services to list, save, delete, and execute named poses
- a JSON catalog topic for ROSClaw/OpenClaw agents
- a CLI for teaching and inspecting poses on the robot host
